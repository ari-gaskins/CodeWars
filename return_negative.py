# given a number, return it as a negative number
# if the number is already negative, return that number unchanged
# if the number is zero, return zero unchanged

def make_negative(number):
    if number <= 0:
        return number

make_negative(0)
make_negative(-1)
make_negative(-2)
